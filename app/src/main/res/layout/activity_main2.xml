<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".ui.MainActivity">

        <TextView
            android:id="@+id/mContentTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"/>

        <Button
            android:id="@+id/mUpdateOrderNoButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="刷新订单号"
            app:layout_constraintTop_toBottomOf="@+id/mBackContentTextView" />

        <Button
            android:id="@+id/mBackButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="跳转"
            app:layout_constraintTop_toBottomOf="@+id/mBackContentTextView" />

        <TextView
            android:id="@+id/mBackContentTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="返回内容显示"
            app:layout_constraintTop_toBottomOf="@+id/mContentTextView"
            app:layout_constraintTop_toTopOf="parent"/>
    </LinearLayout>
</layout>